server.error.whitelabel.enabled=false
server.address=127.0.0.1
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER_URL}
spring.datasource.password=${DB_PASSWORD_URL}

# Localhost
# spring.datasource.url=jdbc:mysql://localhost:3306/sac_banco
# spring.datasource.username=sac-user
# spring.datasource.password=admin123

spring.datasource.driver-class-name=com.mysql.jdbc.Driver

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

spring.jpa.hibernate.naming-strategy=org.hibernate.cfg.ImprovedNamingStrategy
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.mail.host=smtp.gmail.com
spring.mail.port=587

spring.mail.username=${MAIL_LOGIN}
spring.mail.password=${MAIL_PASSWORD}

# Localhost
# spring.mail.username=sacdesenvolvimento@gmail.com
# spring.mail.password=otgoasbzzkjzlvra

# Other properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# TLS , port 587
spring.mail.properties.mail.smtp.starttls.enable=true

# Autenticação
spring.profiles.active=dev
jwt.secret=${JWT_SECRET}

# Localhost
# jwt.secret=sachsafe#4$#21sp

# Um dia de expiração
jwt.expiration=86400000

email.admin=sacdesenvolvimento@gmail.com